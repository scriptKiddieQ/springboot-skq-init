spring:
  application:
    name: springboot-skq-init
  # ?? local ??
  profiles:
    active: local
  # ?????
  # todo ?????
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/default_db
    username: root
    password: 123123
server:
  port: 8123
  servlet:
    context-path: /api
    # cookie 30 ???
    session:
      cookie:
        max-age: 2592000
  #MyBatis Plus ??
mybatis-plus:
  configuration:
    # MyBatis ??
    map-underscore-to-camel-case: false
    # ??????????
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  # ????
  global-config:
    db-config:
      logic-delete-field: isDelete
      logic-delete-value: 1 # ?????
      logic-not-delete-value: 0
    enable-sql-runner: true
# Knife 4j ??????
knife4j:
  enable: true
  openapi:
    title: ????????
    description: ??????????
    concat: scriptKiddieQ
    version: 0.0.1
    group:
      default:
        group-name: ????
        api-rule: package
        api-rule-resources:
          - com.sqk.springbootsqkinit.controller
